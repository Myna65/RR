#include "MeshConverter.h"

#include <OgreMeshManager2.h>
#include <OgreMesh2.h>

void MeshConverter::convert(Mesh &mesh) {

    //Ogre::MeshPtr ogreMesh = Ogre::MeshManager::getSingleton().createManual(mesh.name, "General");

    //Ogre::SubMesh* sub = ogreMesh->createSubMesh();

}